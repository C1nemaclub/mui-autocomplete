// const useSerializer = () => {
//   const serializeContentToText = (content) => {
//     const editor = new Editor({
//       content: content,
//       extensions: [],
//     });

//     const textSerializers = getTextSerializersFromSchema(editor.schema);
//     const doc = editor.schema.nodeFromJSON(editor.getJSON());

//     return textSerializers(doc);
//   };
// };

// export default useSerializer;
